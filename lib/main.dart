// Import helper library from flutter
// to get content on the screen
import 'package:flutter/material.dart'; // => third package import syntax
import 'src/app.dart'; // => import from within lib directory

// Define a 'main' function to run when app starts
void main() {
// Create new text widget to show text on the screen

// Take that widget and get in on the screen
  runApp(App());
}
